import React from 'react';
import Menu from '../Menu/Menu';

const MenuMain = () => {
    return (
        <div style={{display:"flex"}}>
            <Menu/>
        </div>
    );
};

export default MenuMain;